#include "main.h"

/**
 * mul - a function that multiplies two intergers
 * @a: int type number
 * @b: int type number
 * Return: a * b, multiple of a and b
 */

int mul(int a, int b)
{
	return (a * b);
}
